# Note: Databricks Apps run on managed infrastructure with their own package mirror.
# The proxy (var.pip_index_url) is unreachable — do NOT add --extra-index-url here.
databricks-sdk~=0.33.0
fastapi~=0.115.0
uvicorn~=0.32.0
jwcrypto
